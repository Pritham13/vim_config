bind fifo_s sva_checker #(
    .DEPTH(DEPTH),
    .DATA_WIDTH(DATA_WIDTH)
) checker_instance (
    .fifo_din(fifo_din),
    .fifo_rreq(fifo_rreq),
    .fifo_req(fifo_req),
    .aresetn(aresetn),
    .clk(clk),
    .fifo_full(fifo_full),
    .fifo_empty(fifo_empty),
    .fifo_dout(fifo_data_out)
);
